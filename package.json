{
  "name": "smart-billing-system",
  "version": "1.0.0",
  "description": "Smart Billing WebApp — Google Apps Script + Google Sheets (clasp workflow)",
  "scripts": {
    "pull": "clasp pull",
    "push": "clasp push -f",
    "watch": "clasp push --watch",
    "deploy": "node scripts/deploy.js",
    "sync": "node scripts/git-sync.js",
    "open": "clasp open-web-app",
    "editor": "clasp open-script",
    "logs": "clasp logs"
  }
}
